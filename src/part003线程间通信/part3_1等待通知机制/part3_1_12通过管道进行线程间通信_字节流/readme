在java语言中提供了各种各样的输入/输出流stream，使我们能够很方便的对数据进行操作，
其中管道流（pipStream）是一种特殊的流，用于在不同线程间直接传送数据。

一个线程发送数据到输出管道，另一个线程从输入管道中读数据。

通过使用管道，实现不同线程间的通信，而无需借助于类似临时文件之类的东西。

在java的jdk中提供了4个类来使线程间可以进行通信：
1. PipedInputStream和PipedOutStream
2. PipedReader和PipedWriter

